import 'package:flutter/material.dart';

class NursePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nurse Page'),
      ),
      body: Center(
        child: Text('Nurse Content'),
      ),
    );
  }
}