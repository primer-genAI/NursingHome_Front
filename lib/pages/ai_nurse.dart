import 'package:flutter/material.dart';

class AINursePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AINurse Page'),
      ),
      body: Center(
        child: Text('AINurse Content'),
      ),
    );
  }
}